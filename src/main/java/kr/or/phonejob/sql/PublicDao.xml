<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="kr.or.phonejob.pc.Dao.PublicDao">

	<!-- 공통코드 조회(alility_cd) -->
	<select id="abilityService" resultType="kr.or.phonejob.pc.Dto.PublicDto">
		select * from pj_genrl_cd where up_cd='ability_cd' order by cd
	</select>

	<!-- 공통코드 조회(boolean_cd) -->
	<select id="booleanService" resultType="kr.or.phonejob.pc.Dto.PublicDto">
		select * from pj_genrl_cd where up_cd='bolean_cd' order by cd
	</select>

</mapper>